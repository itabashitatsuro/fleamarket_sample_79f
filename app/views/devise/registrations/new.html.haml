.Body
  .Logo
  = link_to image_tag("material/logo/logo.png", alt: "image", class: 'logo-1'), root_path
  .Main
    .New-registration
      %h2 会員情報入力
    .Section
      = devise_error_messages!
      .Box
        = form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: :true do |f|
          .label
            = label :nickname, 'ニックネーム'
            %span.require
              必須
          = f.text_field :nickname, placeholder: "例)フリマ太郎", class: 'input-form'
          .label
            = label :email, 'メールアドレス'
            %span.require
              必須
          = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: " PC・携帯どちらでも可", class: 'input-form'
          .label
            = label :password, 'パスワード'
            %span.require
              必須
          = f.password_field :password, autocomplete: "new-password", placeholder: "7文字以上の半角英数字", class: 'input-form', id:"password"
          .label
            = label :password_confirmation, 'パスワード(確認用)'
            %span.require
              必須
          = f.password_field :password_confirmation, placeholder: "もう一度入力して下さい", class: 'input-form'
          .label
            = label :conformation, '本人確認'
            .Conformation
              安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          .label
            = label :name, 'お名前(全角)'
            %span.require
              必須
          = f.text_field :family_name, placeholder: "例)山田", class: 'input-form'
          = f.text_field :first_name, placeholder: "例)彩", class: 'input-form'
          .label
            = label :name, 'お名前カナ(全角)'
            %span.require
              必須
          = f.text_field :family_name_kana, placeholder: "例)ヤマダ", class: 'input-form'
          = f.text_field :first_name_kana, placeholder: "例)アヤ", class: 'input-form'
          .label
            = label :birthday, '生年月日'
            %span.require
              必須
            .BirthDay
              != sprintf(f.date_select(:birthday,with_css_classes:'input-form', prompt:"--",use_month_numbers:true, start_year:Time.now.year, end_year:1900, date_separator:'%s'),'年','月')+'日'
            .Attention
              ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          = f.submit "登録する", class: 'btn-default btn-mail'
= render 'layouts/footer'