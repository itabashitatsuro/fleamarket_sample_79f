.credit_cards-show
  .credit_cards-show-frame
    .credit_cards-show-title
      登録クレジットカード情報
    .credit_cards-list
      .credit_cards-list-frame
        .credit_card_info
          .credit_card_info-brand
            / showメソッドで定義した@card_srcを呼び出し
            = image_tag "/assets/images/credit_cards/#{@card_src}", class: "credit_card_info__brand__img", alt: @card_brand
          .credit_card_info-numbers
            .credit_card-number
              000000000000
              / Pay.jpから取得したカード番号の末尾４桁を取得
              = "**** **** **** " + @customer_card.last4
            .expiration_date
              .expiration_date__title
                有効期限
              .expiration_date__info
                / showメソッドで定義した有効期限の年と月を取得
                = @exp_month + " / " + @exp_year
        .destroy__function
          .destroy__function__link
            / 削除ボタンを作成、押すとdestroyメソッドが働くように
            = link_to credit_card_path, method: :delete, class: "destroy-function-link-btn" do 
              削除する
     